function problem3(number) {
  let answer = 0;

  /* 숫자를 자릿수별 배열로 저장 */
  for (let i = 1; i <= number; i++) {
    let numberArr = String(i).split("");
  }

  return answer;
}
module.exports = problem3;
